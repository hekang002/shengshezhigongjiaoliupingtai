# 湖北供销·心声登录页设计

## 目标

在独立的“原型优化设计”目录提供可直接用浏览器打开的 HTML 登录原型。登录页同时演示职工、承办、管理员、领导四类用户的入口，并在登录后加载各自的 Mock 工作台。

## 视觉方向

采用双栏政务工作台风格：左侧为深墨色品牌叙事区，以供销红作为印章、主操作与强调色；右侧为克制的白色登录面板。当前原型聚焦桌面端，突出机构身份与安全提示。

## 登录、注册与审批交互

- 账号密码登录：页面默认填入职工演示账号 `staff` 和密码 `123456`；待审核或已驳回账号会显示审批状态，不能进入系统。
- 验证码登录：输入手机号和演示短信验证码后进入默认职工工作台。
- 四个角色卡片会自动填入对应账号与密码，点击后可直接登录。
- 账号密码错误或手机号、短信验证码不合法时，在表单内给出明确错误提示。
- 登录页入口名称为“账号申请与审核查询”，用于提交注册申请及查询已有手机号的审核状态。
- 注册页需要录入手机号码、登录密码、确认密码与短信验证码。密码少于 6 位、两次密码不一致、手机号或验证码错误时阻止提交。
- 提交注册后创建一条“待审核”职工账号申请，并返回登录页显示“申请已提交，等待管理员审核”。
- 忘记密码页录入手机号码、短信验证码、新密码与确认密码；只有已审核通过的账号可提交密码重置。

## 审批状态流

`注册填写` -> `待管理员审核` -> `审核通过` -> `可以登录`

管理员可在 Mock 管理端的“账号审核”模块查看待审核申请，并执行“通过”或“驳回”。通过后，该手机号可使用密码登录和验证码登录；驳回后，登录页提示驳回状态。原型中提交的新申请不会真实持久化，页面刷新后恢复预置 Mock 数据。

## Mock 数据

| 角色 | 演示账号 | 密码 | 登录后首屏模块 |
| --- | --- | --- | --- |
| 职工 | `staff` | `123456` | 待办提醒、我的发言、热门交流、近期公告 |
| 承办 | `handler` | `123456` | 待办理事项、即将超期、答复草稿、办理进度 |
| 管理员 | `admin` | `123456` | 待审核内容、待分办事项、平台用户、今日访问 |
| 领导 | `leader` | `123456` | 运行概览、事项闭环率、热点议题、重点事项 |

验证码登录使用手机号 `13800002026` 和短信验证码 `202608`，进入职工工作台。

另预置两条审批申请：`13900002026`（待审核）和 `13700002026`（已驳回），用于演示登录拦截与管理端审核。所有演示密码均为 `123456`。

## 工作台范围

登录后为单页 Mock 工作台，不复刻原参考项目的完整菜单。顶部可切换四类角色；每个角色显示 4 个与职责相符的概览指标、任务/内容列表和一项主操作。管理端额外提供账号审核列表。退出登录回到登录页。

## 验收

1. 双击 `index.html` 即可运行，无构建或服务端依赖。
2. 账号密码和验证码两种登录方式均可验证并给出错误反馈。
3. 可完成注册申请、忘记密码和管理员审核通过/驳回的 Mock 闭环；未审核账号不得登录。
4. 四个角色演示入口及登录后的模块数据可交互切换。
5. 页面在 1440px 宽度的桌面浏览器下完整展示，不出现水平溢出。

## Delivery Verification

已在本地 HTTP 服务中验证：登录页可正常打开，默认填入 `staff / 123456`；管理员可审核通过预置的 `13900002026` 待审核账号；该账号退出后可使用密码 `123456` 进入职工工作台。账号申请与审核查询页可识别该手机号的“待审核”状态，1440px 桌面视口没有水平溢出。
