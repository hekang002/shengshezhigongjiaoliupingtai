# 管理端侧栏点击式抽屉设计

所有角色的左侧导航统一为两级点击式抽屉。默认只显示各一级模块，均不展开；点击一级模块展开对应二级页面，再点收起，打开另一一级模块时关闭之前展开的模块。点击二级页面后保持所在模块展开，页面内快捷入口跳转时自动展开目标所在模块。角色切换重置为默认折叠状态。当前页面的一级模块保留激活提示，二级页面保留红色选中样式。采用现有桌面侧栏与品牌红色样式，不改变业务页面和职工端登录。
